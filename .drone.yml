kind: pipeline
type: docker
name: default

steps:
  - name: test
    image: maven:3-jdk-10
    commands:
      - echo start package
#      - mvn clean package -DskipTests=true -Dmaven.javadoc.skip=true -B -V
#      - mvn test -B