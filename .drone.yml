kind: pipeline
type: exec
name: deployment

platform:
  os: linux
  arch: amd64

steps:
  - name: greeting
    commands:
    - echo hello world
    - echo DRONE_LOG_FILE
    when:
      branch:
        - main