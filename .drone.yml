kind: pipeline
name: default
type: docker

steps:
  - name: 单元测试
    image: maven:3-jdk-8
    volumes:
      - name: cache
        path: /.m2
    commands:
      - mvn clean package -DskipTests=true -Dmaven.javadoc.skip=true -B -V
      - mvn test -B